{
  "name": "cognito-s3-cloudformation-example",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:dev": "rm -rf dist && webpack --devtool source-map --mode=development && cp src/index.html dist/",
    "build:prod": "rm -rf dist && webpack --mode=production && cp src/index.html dist/"
  },
  "keywords": [],
  "author": "Franz Wong",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "json-loader": "^0.5.7",
    "webpack": "^4.29.0",
    "webpack-cli": "^3.2.1"
  },
  "dependencies": {
    "@aws-amplify/auth": "^2.1.5",
    "aws-sdk": "^2.625.0",
    "escape-html": "^1.0.3"
  }
}
